import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
  cabecalho: {
    flexDirection: "row",
    alignItems: "center"
  },

  fotoPerfil: {
    width: 50,
    height: 50,
    borderRadius: 30,
    margin: 10
  }
});

export default estilos;
